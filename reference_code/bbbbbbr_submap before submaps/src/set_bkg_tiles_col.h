
#include <types.h>

void set_bkg_tiles_col(UINT8 x,
          UINT8 y,
          UINT8 col_rows,
          UINT8 src_map_width,
          const unsigned char *tiles) NONBANKED __preserves_regs(b, c);

